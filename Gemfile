#Talar om för bundler var biblioteken vi ska använda finns
#kör "bundle install" i terminalen i samma mapp för att ladda ner biblioteken

source 'https://rubygems.org'

gem 'rack'
gem 'rackup'
gem 'sinatra'
gem 'sqlite3'
gem 'webrick'
gem 'awesome_print'
gem 'debug'
gem 'rake'

group :development do
  gem 'sinatra-contrib' # Replaces 'rerun' for better Ruby 4.0 compatibility
end